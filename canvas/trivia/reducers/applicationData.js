import _ from 'lodash'

const applicationData = (state = { 
	checksum: null,
	canvasId: null,
	appId: null,
}, action) => {
	switch (action.type) {
		default:
			return state
	}
}

export default applicationData