const applicationData = (
	state = {
		title: null,
		checksum: null,
		canvasId: null,
		appId: null,
	},
	action
) => {
	switch (action.type) {
		default:
			return state
	}
}

export default applicationData
