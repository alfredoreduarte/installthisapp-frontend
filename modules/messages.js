const photo_contest = require('modules/photo_contest/messages').default
const trivia = require('modules/trivia/messages').default

const dictionary = {
	photo_contest,
	trivia,
}

export default dictionary