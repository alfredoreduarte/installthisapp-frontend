<script>
	// This empty object emulates segment.com's js library
	// to disable every single tracking function when inspecting a user
	window.Samestate = function() {
		console.log('Fired Samestate')
	}
	window.analytics = {
		page: (string, options, callback) => {
			console.log('segment: page("' + string +'") called')
			if (typeof options === "function") {
				options()
			}
			if (typeof callback === "function") {
				callback()
			}
		},
		identify: (string, options, callback) => {
			console.log('segment: identify("' + string +'") called')
			if (typeof options === "function") {
				options()
			}
			if (typeof callback === "function") {
				callback()
			}
		},
		track: (string, options, callback) => {
			console.log('segment: track("' + string +'") called')
			if (typeof options === "function") {
				options()
			}
			if (typeof callback === "function") {
				callback()
			}
		},
	}
</script>