const applicationLog = (
  state = {
    designEdited: false,
    fbTabInstalled: false,
    appInstalled: false,
  },
  action
) => {
  switch (action.type) {
    default:
      return state
  }
}

export default applicationLog
